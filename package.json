{
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm turbo build",
    "commit": "lint-staged && git-cz",
    "dev": "lerna run --parallel dev",
    "fix": "eslint --debug --fix packages || true && prettier --write \"packages/**/*\"",
    "lint": "eslint --debug ./packages",
    "test": "FORCE_COLOR=1 vitest --coverage $VITEST_EXTRA_OPTS",
    "test:storybook": "pnpm exec playwright install chromium && test-storybook --coverage --config-dir packages/config/storybook-config/.storybook --maxWorkers=4 && nyc report --reporter=lcov -t coverage/storybook --report-dir coverage/storybook",
    "turbo": "TURBO_TELEMETRY_DISABLED=1 FORCE_COLOR=1 turbo --cache-dir ./.turbo-cache --color  --no-daemon"
  },
  "devDependencies": {
    "@fuf-stack/eslint-config-fuf": "1.5.0",
    "@fuf-stack/project-cli-tools": "1.0.17",
    "@fuf-stack/typescript-config": "0.4.0",
    "@fuf-stack/vitest-config": "4.1.3",
    "@repo/storybook-config": "workspace:*",
    "@repo/tailwind-config": "workspace:*",
    "@repo/tsup-config": "workspace:*",
    "@repo/tsdown-config": "workspace:*",
    "@repo/vite-config": "workspace:*",
    "@types/node": "24.10.9",
    "color2k": "2.0.3",
    "jsdom": "27.4.0",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vite-tsconfig-paths": "6.0.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional",
      "@commitlint/config-pnpm-scopes"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
