import Table, { tableVariants } from './Table';

export type {
  TableProps,
  TableRowProps,
  TableColumnProps,
  VariantProps,
} from './Table';

export { Table, tableVariants };

export default Table;
